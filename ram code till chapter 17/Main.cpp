#include "Engine.h"

int main()
{
   // Declare an instance of Engine;
	Engine engine;

	// Start the engine VRRrrrmmm;
	engine.run();

	// Quit in the usual way when the eingine is stopped;
	return 0;
}
